/**
 * Rhs panel to show the Description on the UI for the selected Node
 */

const NodeDescription = (props) => {
  return <div>Sample description for the selected node</div>;
};

export default NodeDescription;
